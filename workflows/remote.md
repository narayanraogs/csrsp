# Remote

Remote is a special mode of operation

Whenever a special remote client is connected to the server all the connected clients will display a switch to remote/local mode in the ui.

If the user switces to remote, it shows the current remote operation. The currently supported remote operation is only acquisition, so it flows between acquisition, intermediate reports and result display screen.