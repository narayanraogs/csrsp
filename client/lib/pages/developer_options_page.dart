import 'package:flutter/material.dart';

class DeveloperOptionsView extends StatelessWidget {
  const DeveloperOptionsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Developer Options Content'),
    );
  }
}