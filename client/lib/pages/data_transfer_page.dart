import 'package:flutter/material.dart';

class DataTransferView extends StatelessWidget {
  const DataTransferView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Data Transfer Content'),
    );
  }
}
