import 'package:flutter/material.dart';

class ResultProfilesView extends StatelessWidget {
  const ResultProfilesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Result Profiles Content'),
    );
  }
}