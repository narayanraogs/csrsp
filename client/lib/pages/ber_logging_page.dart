import 'package:flutter/material.dart';

class BerLoggingView extends StatelessWidget {
  const BerLoggingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BER Logging Content'),
    );
  }
}