import 'package:flutter/material.dart';

class OfflineProcessingView extends StatelessWidget {
  const OfflineProcessingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Offline Processing Content'),
    );
  }
}