import 'package:flutter/material.dart';

class AcquireDataView extends StatelessWidget {
  const AcquireDataView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Acquire Data Content'),
    );
  }
}