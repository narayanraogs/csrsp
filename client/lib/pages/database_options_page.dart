import 'package:flutter/material.dart';

class DatabaseOptionsView extends StatelessWidget {
  const DatabaseOptionsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Database Options Content'),
    );
  }
}